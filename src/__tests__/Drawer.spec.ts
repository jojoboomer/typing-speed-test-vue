import { describe, expect, it } from 'vitest'

describe('Drawer', () => {
  it('is a valid component (smoke test)', () => {
    // Drawer requires DOM and Teleport context, verified in integration tests
    expect(true).toBe(true)
  })
})
